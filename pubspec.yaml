name: data_analyzer_app
description: 数据分析仪，离线购买情况号码分析与相似组合生成 APK。
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: 8.3.7
  path_provider: 2.1.4
  share_plus: 12.0.1
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
